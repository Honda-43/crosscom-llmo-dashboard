# Phase 5 実装指示書 — ダッシュボードを「8面レポート構成」へ再編

対象リポジトリ:crosscom-llmo-dashboard
目的:ローカル分析アプリを、承認済みの完成イメージ(8面レポート)と同じ構成に
再編する。数値の羅列ではなく「状態→原因→アクション承認」の順で意思決定が
完了する画面にする。**2026-08-31(月)の週次レビューまでに完成させる。**

既存の§2プロンプト・§4抽出スキーマ・既存タブのスキーマは変更禁止。
実行系(daily/weekly)への変更は本指示書で明示した追記のみ。

---

## 1. アプリの画面構成(5ページ→8面構成へ再編)

サイドバーを以下の8面にする。既存実装は捨てずに再配置する。

| 面 | 名称 | 中身 | 既存流用 |
|----|------|------|---------|
| R1 | 全体サマリ | 最重要指標カード4枚+今週の判定欄 | P1のカードを再構成 |
| R2 | 言及率トレンド | 移動平均3系列+施策実施日の縦線注釈+判定欄 | P1のグラフ+注釈を追加 |
| R3 | ネガ検知 | モデル別日次カレンダー+施策基準線+判定欄 | P1のカレンダーを拡張 |
| R4 | 獲得マップ | prompt×modelヒートグリッド(直近7日言及日数)+判定欄 | P1のグリッド流用 |
| R5 | 競合ポジション | 散布図(新設)+シェアランキング横棒+判定欄 | P2を再構成 |
| R6 | 情報源分析 | E-1引用の推移+不在引用元(新設)+判定欄 | P3の一部を統合 |
| R7 | KGI | 週計カード+波及順序の表示+判定欄 | 新設(データは既存) |
| R8 | アクションボード | action_log一覧+状態管理 | P5を置換 |

- 旧P3(プロンプト詳細)と旧P4(回答ビューア・差分)は「詳細」セクションとして
  サイドバー下部に残す(R4・R6からの深掘り先)
- 各面の見た目は完成イメージに準拠:白カード・前週比チップ・
  下部に判定欄(§2)

## 2. 判定欄の自動生成(全面共通・最重要仕様)

各面の下部に「判定:」で始まる1〜3文の判定欄を表示する。
**生成はLLMを使わず、決定的なテンプレートで行う**(rules_engineの発火結果と
閾値判定の組み合わせ)。テンプレート定義は config/verdict_templates.yaml に
外出しし、条件→文面のマッピングをコードから分離する。

例(R3 ネガ検知):
- 条件: negative継続日数>0 かつ 最新施策日からの経過日数<閾値(28日)
  → 「{施策名}から{N}日、AIの参照面はまだ入れ替わっていない(想定内)。
     {判断期限}を判断期限とし、それまでに検知が止まらなければ
     {次の施策}を発動する。」
- 条件: 直近7日の検知0件 → 「検知停止。{施策名}の効果が
  {実施日から何日}で反映されたと判定。」

各面に最低2分岐(正常系/要対応系)を定義する。判断期限・次の施策は
action_log(§4)から取得する。数値はすべてstats/シートの実値のみ使用し、
テンプレートにない文章を生成しない。

## 3. 新設チャート2点

### 3-1 競合ポジション散布図(R5)
- X軸=言及シェア(直近28日の出現数/観測数)、Y軸=順位中央値(軸反転・上が上位)、
  点サイズ=直近7日の出現数
- クロスコムは赤・大きめ・ラベル固定表示。他は上位10社+ホバーで社名
- 四分割の破線と象限ラベル:「高シェア×上位」「高シェア×下位」
  「低シェア×上位」「低シェア×下位」
- データ源:sov_daily+llm_observationsのrank(orderは中央値、
  言及なし期間は除外)

### 3-2 不在引用元(R6)
- llm_observationsのall_cited_urls(参照できない場合はdata/rawから再構成)を
  ドメイン単位で集計し3分類:
  「自社」(cross-com.jp)/「共通」(自社言及ありの回答でも引用される外部)/
  「自社不在」(自社mention=FALSEの回答でのみ引用される外部)
- 分類別に引用回数ランキング表示。「自社不在」上位は
  『掲載依頼先の候補』の見出しで強調
- 集計結果はシートタブ `citation_gap` に週次で書き出す(weekly.ymlに追記):
  | date | domain | category | cited_count | prompts |

## 4. action_log(施策記録)の新設

シートタブ `action_log` を新設し、アプリR8で一覧表示・状態は本田さんが
シート上で直接編集する運用(アプリからの書き込みは実装しない):

| action_id | 優先度 | 内容 | 対象 | 根拠rule_id | 状態 | 提案日 | 実施日 | 判断期限 |

状態の値: 提案中 / 承認待ち / 承認 / 実施済み・効果測定中 / 完了 / 却下 / 保留

**初期データとして以下を投入する:**
| A-001 | 高 | AUBA・イプロスの外部プロフィール更新 | E-1 | R-P7 | 実施済み・効果測定中 | 2026-08-08 | 2026-08-11 | 2026-09-07 |
| A-002 | 高 | /marketing-automation-btob/ 過去形化改修 | E-1 | R-P7 | 実施済み・効果測定中 | 2026-08-17 | 2026-08-18 | 2026-09-07 |
| A-003 | 高 | /btob-marketing-strategy/ 過去形化改修 | E-1 | R-P8 | 実施済み・効果測定中 | 2026-08-24 | 2026-08-24 | 2026-09-07 |
| A-004 | 高 | /about/ Organization構造化データ実装 | E-1 | R-P8 | 実施済み・効果測定中 | 2026-08-24 | 2026-08-24 | — |
| A-005 | 中 | 問い合わせフォームに認知経路を追加 | KGI | — | 実施済み・効果測定中 | 2026-08-24 | 2026-08-24 | — |
| A-006 | 中 | 外部プロフィール第2弾(ランサーズ・innovations-i・PR TIMES) | E-1 | R-P7 | 保留 | 2026-08-24 | — | 2026-09-07 |
| A-007 | 中 | B-3対応の一次情報ページ更新 | B-3 | R-P2 | 保留 | 2026-08-24 | — | 2026-08-31 |

**実施日はR2・R3のグラフに縦線注釈として自動描画する**(実施済み以降の
状態のもののみ。ラベルは内容の先頭12字)。

## 5. 週次所見との連携(実行系への追記)

generate_insight の出力後処理として、所見の「推奨アクション」を
action_log に「提案中」で追記する(重複防止:同一内容+同一rule_idが
未完了状態で存在する場合は追記しない)。所見文の形式は変更しない。

## 6. board_daily タブ(Looker Studio刷新の準備)

run_daily.py の末尾に、Looker Studio用フラットタブ `board_daily` の
書き出しを追加(冪等):
| date | mention_rate_all_7d | mention_rate_a_7d | mention_rate_b_7d |
| sov_rank | sov_share | negative_streak_days | branded_clicks_wk |
| ai_sessions_wk | noise_flag | material_events(セミコロン区切り要約) |

※Looker Studio自体の再構築は本田さんの手作業のため本Phaseの対象外。
タブだけ先に用意する。

## 7. Definition of Done

1. run_dashboard.bat 起動でR1〜R8が実データで表示される
2. 各面に判定欄が表示され、文面がテンプレート由来である
   (verdict_templates.yamlの変更だけで文面が変わることをテストで確認)
3. R2・R3にA-001〜A-005の縦線注釈が出る
4. citation_gap・action_log・board_daily タブが作成されデータが入る
5. 全8面のスクリーンショット付きで完了報告
6. pytest既存全件+新規テスト(判定テンプレート分岐・不在引用元の3分類・
   action_log重複防止)がpass
7. 2026-08-31の朝までにmainへpush完了

## 8. 実装時の判断ルール

- 新規有料サービス禁止・実行系のSheets APIコール増は最小化
  (読み取りの共有・キャッシュ徹底)
- 判定欄テンプレートの文面はこの指示書の例に進拠しつつ調整可。ただし
  「比喩を使わない・英語表記を使わない(製品名/システム名を除く)・
  数値は実値のみ」を厳守
- 完成イメージ(8面ビジュアル)と本指示書が矛盾する場合は本指示書を優先し、
  判断に迷う点は実装を止めて報告
